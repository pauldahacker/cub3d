#include <mlx.h>
#include <stdio.h>
#include <stdlib.h>
/*
int main(void)
{
    void *mlx;
    void *window;
    void *img;
    int img_width, img_height;

    mlx = mlx_init();
    window = mlx_new_window(mlx, 800, 600, "Affichage d'une image XPM");
    img = mlx_xpm_file_to_image(mlx, "./textures/wall_1.xpm", &img_width, &img_height);
    mlx_put_image_to_window(mlx, window, img, 0, 0);
    mlx_loop(mlx);
    return EXIT_SUCCESS;
}*/
